header.header
    .container-fluid
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href=`${prefixAdmin}/dashboard`) ADMIN FOOD
                    
            .col-9
                div(class="text-right")
                    a(
                    href=prefixAdmin + '/logout'
                    class="btn btn-danger btn-sm"
                    ) Đăng xuất